# ✅ Интеграция 1Plat — Завершено!

## 📋 Что было сделано

### 1️⃣ Новые файлы

- ✅ **`services/oneplat.py`** — Сервис для работы с 1Plat API
  - Создание платежей через `/api/merchant/order/create/by-api`
  - Получение информации о платеже
  - Верификация коллбеков по подписям (signature_v2)
  - Активация подписки при успешной оплате

- ✅ **`webhook_server.py`** — HTTP сервер для приёма вебхуков
  - Слушает на `0.0.0.0:8080`
  - Маршрут: `POST /1plat-webhook`
  - Обрабатывает коллбеки от 1Plat
  - Возвращает 200/201 для подтверждения

- ✅ **`ONEPLAT_INTEGRATION.md`** — Полная документация по интеграции

### 2️⃣ Обновлённые файлы

**`config.py`** — Добавлены переменные конфигурации:
```python
ONEPLAT_SHOP_ID
ONEPLAT_SHOP_SECRET  
ONEPLAT_API_URL
ONEPLAT_WEBHOOK_URL
```

**`.env`** — Добавлены переменные окружения:
```env
ONEPLAT_SHOP_ID=YOUR_SHOP_ID
ONEPLAT_SHOP_SECRET=YOUR_SHOP_SECRET
ONEPLAT_API_URL=https://1plat.cash
ONEPLAT_WEBHOOK_URL=https://spn.bot.idlebat.online/1plat-webhook
```

**`database.py`** — Добавлены функции и миграции:
- `run_migrations()` — добавлены столбцы `guid` и `payment_status` в таблицу payments
- `create_oneplat_payment()` — сохранение платежа 1Plat в БД
- `get_payment_by_guid()` — получение платежа по guid
- `update_payment_status_by_guid()` — обновление статуса платежа

**`handlers/subscription.py`** — Обновлены обработчики:
- Заменил "💳 Yookassa" на "💳 1Plat (Карта/СБП)"
- Добавил обработчик `process_pay_1plat()` для создания платежей

**`main.py`** — Добавлен вебхук-сервер:
- Импорт `start_webhook_server` и `stop_webhook_server`
- Запуск вебхука при старте бота
- Остановка вебхука при выключении

### 3️⃣ Логика работы

```
1. Пользователь выбирает "Оформить подписку"
   ↓
2. Выбирает тариф (1m, 3m, 6m, 12m)
   ↓
3. Выбирает способ оплаты: "CryptoBot" или "1Plat (Карта/СБП)"
   ↓
4. Если выбрал 1Plat:
   - Генерируется ID платежа (merchant_order_id)
   - Создаётся платёж через 1Plat API (POST /api/merchant/order/create/by-api)
   - Платёж сохраняется в БД со статусом "pending"
   - Отправляется ссылка на оплату пользователю
   ↓
5. Пользователь оплачивает через форму 1Plat
   ↓
6. 1Plat отправляет коллбек на https://spn.bot.idlebat.online/1plat-webhook
   ↓
7. Вебхук-сервер:
   - Проверяет подпись (signature_v2)
   - Получает платёж из БД по guid
   - Если статус = 1 или 2 (оплачено):
     * Активирует подписку в Remnawave
     * Обновляет БД
     * Возвращает 200 OK
   ↓
8. Пользователь получает подписку ✅
```

## 🔐 Безопасность

### Верификация подписей

1Plat отправляет коллбек с двумя подписями:
- **signature** — HMAC-SHA256(JSON, secret)
- **signature_v2** — MD5(merchantId + amount + shopId + secret)

Код проверяет **signature_v2**:

```python
def verify_callback_signature_v2(merchant_id, amount, shop_id, shop_secret, signature_v2):
    data = f"{merchant_id}{amount}{shop_id}{shop_secret}"
    expected_sig = hashlib.md5(data.encode()).hexdigest()
    return expected_sig == signature_v2
```

### Активация подписки только по коллбеку

- ❌ Подписка НЕ активируется при создании платежа
- ✅ Подписка активируется ТОЛЬКО после валидного коллбека от 1Plat
- ✅ Коллбек проверяется по подписи

## 📦 Установка

### Локально

```bash
# Обновить код с гита
git pull origin main

# Установить зависимости (если нужны новые)
pip install -r requirements.txt

# Обновить .env файл
# Добавить ONEPLAT_SHOP_ID и ONEPLAT_SHOP_SECRET
```

### На VPS

```bash
# Подключиться
ssh root@YOUR_IP

# Перейти в проект
cd ~/spn-vpn-bot

# Скачать обновления
git pull origin main

# Активировать виртуальное окружение
source venv/bin/activate

# Обновить зависимости (если есть новые)
pip install -r requirements.txt --upgrade

# Выйти из virtualenv
deactivate

# Обновить .env
nano .env
# Добавь:
# ONEPLAT_SHOP_ID=...
# ONEPLAT_SHOP_SECRET=...

# Перезагрузить бота
sudo systemctl restart spn-bot

# Проверить статус
sudo systemctl status spn-bot

# Смотреть логи
sudo journalctl -u spn-bot -f
```

## ✅ Проверка работы

### В логах должны видеть

```
Database pool initialized successfully
Running migrations...
Migration executed: ALTER TABLE payments ADD COLUMN IF NOT EXISTS guid TEXT UNIQUE;
Migration executed: ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_status TEXT DEFAULT 'pending';
All migrations completed successfully ✅
Webhook server started for 1Plat callbacks on 0.0.0.0:8080
All handlers registered
Bot started polling...
```

### Тестирование платежа

1. Напиши боту `/start`
2. Нажми "💳 Оформить подписку"
3. Выбери тариф
4. Выбери "💳 1Plat (Карта/СБП)"
5. Нажми "💳 Оплатить картой/СБП"
6. Перейди по ссылке и оплати тестовой картой
7. После оплаты должна активироваться подписка

## 📊 Структура платежей

### В БД (таблица payments)

```
id                SERIAL PRIMARY KEY
tg_id            BIGINT (ID пользователя)
tariff_code      TEXT (1m, 3m, 6m, 12m)
amount           DECIMAL (сумма платежа)
status           TEXT ('pending', 'completed')
created_at       TIMESTAMP
provider         TEXT ('cryptobot', '1plat')
invoice_id       TEXT (merchant_order_id для 1Plat)
guid             TEXT UNIQUE (глобальный ID платежа от 1Plat)
payment_status   TEXT (статус платежа от 1Plat: 0, 1, 2)
```

### Коллбек от 1Plat

```json
{
  "signature": "...",           // подпись HMAC-SHA256
  "signature_v2": "...",        // подпись MD5
  "payment_id": "123",          // ID платежа у 1Plat
  "guid": "111-222-333",        // глобальный ID платежа
  "merchant_id": "543",         // ID магазина
  "user_id": "1111",            // ID пользователя
  "status": 1,                  // 0=ожидает, 1=оплачен, 2=подтверждён
  "amount": 100,                // сумма в рублях
  "amount_to_pay": 100,         // сумма к оплате
  "amount_to_shop": 85,         // сумма к магазину (после комиссии)
  "expired": "2025-04-11T..."   // время истечения платежа
}
```

## 🚀 Что дальше?

### Необходимо в ЛК 1Plat

1. ✅ Получить Shop ID и Secret
2. ⚠️ Настроить вебхук: https://spn.bot.idlebat.online/1plat-webhook
3. ⚠️ Верифицировать домен (TXT запись, файл или meta тег)
4. ⚠️ Протестировать платёж

### Опциональное улучшение

- Добавить обработчик СБП специально (метод "sbp")
- Добавить обработчик крипто (метод "crypto")
- Добавить проверку лимитов платежей (min/max)
- Добавить повторную отправку коллбеков при ошибке

## 📞 Полезные ссылки

- **1Plat Документация**: https://1plat.cash/docs
- **1Plat ЛК**: https://lk.1plat.cash
- **Поддержка**: support@1plat.cash

## 🔍 Файлы для изучения

Если хочешь разобраться глубже:

1. **`services/oneplat.py`** — Вся логика API
2. **`webhook_server.py`** — Обработка вебхуков
3. **`handlers/subscription.py`** — UI и создание платежей
4. **`database.py`** — Работа с БД
5. **`ONEPLAT_INTEGRATION.md`** — Полная документация

---

**Status: ✅ READY FOR DEPLOYMENT**

Проект готов к развёртыванию на VPS!
