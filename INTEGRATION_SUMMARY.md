# ✨ Интеграция 1Plat - Краткое резюме

## Что было сделано

Вместо заглушки для оплаты через Yookassa реализована **полная интеграция платежной системы 1Plat**.

### Основные возможности

| Возможность | Описание |
|------------|---------|
| 💳 **Карты** | VISA, MasterCard, МИР |
| 📱 **СБП** | Платежи через систему быстрых платежей |
| ✅ **Webhook** | Автоматическое получение callback'ов от 1Plat |
| 🔐 **Подписи** | Проверка HMAC-SHA256 и MD5 подписей |
| 🔒 **Безопасность** | Активация подписки ТОЛЬКО после оплаты |

---

## Структура изменений

### 📁 Новые файлы

```
services/
└── oneplat.py                    # Сервис 1Plat API

handlers/
└── webhooks.py                   # Webhook обработчик

Документация:
├── 1PLAT_INTEGRATION.md         # Полная документация
├── ONEPLAT_DEPLOYMENT.md        # Инструкция развёртывания
├── INTEGRATION_SUMMARY.md        # Этот файл
└── test_1plat_webhook.py        # Тестовый скрипт
```

### ✏️ Обновлённые файлы

```
config.py                         # +4 переменные для 1Plat
database.py                       # +миграция и 4 функции
handlers/subscription.py          # Заменена Yookassa на 1Plat
states.py                         # +1 состояние
main.py                          # +Quart webhook сервер
services/cryptobot.py            # Обновлена для обоих провайдеров
requirements.txt                 # +quart>=0.18.0
.env.example                     # +4 переменные
```

---

## Как это работает

### Пользовательский поток

```
1. Пользователь выбирает тариф подписки
2. Выбирает "Оплата" (раньше Yookassa)
3. Выбирает способ: 💳 Карта или 📱 СБП
4. Бот создаёт платеж через API 1Plat
5. Показывает реквизиты для оплаты:
   - Номер карты
   - ФИО получателя
   - Банк
6. Пользователь оплачивает
7. 1Plat отправляет callback на webhook
8. Бот получает callback на /1plat-webhook
9. Проверяет подпись (безопасность)
10. Активирует подписку в Remnawave
11. Отправляет ссылку пользователю
```

### Архитектура

```
Telegram Bot (Aiogram)
├── handlers/subscription.py
│   └── Создаёт платеж через create_oneplat_payment()
├── services/oneplat.py
│   └── API запросы к 1Plat
├── Quart webhook сервер (порт 8080)
│   └── handlers/webhooks.py → /1plat-webhook
└── database.py
    └── Сохраняет платежи и активирует подписку
```

---

## Переменные окружения

Добавить в `.env`:

```env
# 1Plat магазин ID
ONEPLAT_SHOP_ID=12345

# Секретный ключ магазина
ONEPLAT_SHOP_SECRET=your_shop_secret_key

# URL API 1Plat
ONEPLAT_BASE_URL=https://1plat.cash

# URL для callback'ов (вебхука)
ONEPLAT_CALLBACK_URL=https://spn.bot.idlebat.online/1plat-webhook
```

---

## Статусы платежей 1Plat

| Статус | Код | Действие |
|--------|-----|---------|
| Ошибка | -2 | Игнорировать |
| Черновик | -1 | Ждать выбора метода |
| Ожидает оплаты | 0 | Ждать оплаты |
| Оплачен* | 1 | **Активировать подписку** ✅ |
| Подтвержден* | 2 | **Активировать подписку** ✅ |

\* Подписка активируется при статусе 1 или 2

---

## Безопасность: Проверка подписей

### Метод 1: HMAC-SHA256
```python
signature = HMAC-SHA256(JSON.stringify(payload), shopSecret)
```

### Метод 2: MD5
```python
signature_v2 = MD5(merchantId + amount + shopId + shopSecret)
```

**Реализация в коде:**
```python
def verify_callback(body):
    v1_valid = verify_signature_v1(body)
    v2_valid = verify_signature_v2(body)
    return v1_valid or v2_valid  # Валидна если одна из них подходит
```

---

## Развёртывание на новом сервере

### 1️⃣ SSH подключение
```bash
ssh root@77.233.214.150
```

### 2️⃣ Клонирование
```bash
cd /opt
git clone https://github.com/Hamzat-Yaudarov/spn-vpn-bot.git
cd spn-vpn-bot
```

### 3️⃣ Виртуальное окружение
```bash
python3.10 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4️⃣ Переменные окружения
```bash
cp .env.example .env
nano .env  # Заполните всё, включая ONEPLAT_*
```

### 5️⃣ Проверка БД
```bash
python -c "import asyncio; from database import init_db; asyncio.run(init_db())"
```

### 6️⃣ Запуск
```bash
python main.py
```

**Результат:**
- Telegram polling запущен
- Webhook сервер слушает на порте 8080

### 7️⃣ Настройка 1Plat
1. Перейти в ЛК 1Plat
2. Найти Shop ID и Secret Key
3. Поместить в `.env`
4. Установить callback URL: `https://spn.bot.idlebat.online/1plat-webhook`
5. Верифицировать домен

### 8️⃣ Настройка Nginx
```nginx
location /1plat-webhook {
    proxy_pass http://localhost:8080;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
}
```

### 9️⃣ SSL сертификат
```bash
sudo certbot certonly --nginx -d spn.bot.idlebat.online
```

### 🔟 Systemd сервис
```bash
sudo nano /etc/systemd/system/spn-bot.service
# Следовать инструкции в ONEPLAT_DEPLOYMENT.md
sudo systemctl enable spn-bot
sudo systemctl start spn-bot
```

---

## Тестирование

### Локальное тестирование webhook'а

```bash
# Убедитесь, что бот запущен:
python main.py

# В другом терминале:
python test_1plat_webhook.py 1    # Статус "оплачено"
```

Скрипт отправит тестовый callback с правильными подписями.

### Проверка логов
```bash
# Если используется systemd:
sudo journalctl -u spn-bot -f | grep 1plat

# Если запущен в foreground:
tail -f bot.log | grep 1plat
```

---

## Файлы и их назначение

### Основной код

| Файл | Что делает |
|------|-----------|
| `services/oneplat.py` | API запросы к 1Plat |
| `handlers/webhooks.py` | Получение callback'ов |
| `handlers/subscription.py` | Показ платежных способов |
| `database.py` | Сохранение платежей |
| `config.py` | Переменные окружения |

### Документация

| Файл | Что в нём |
|------|-----------|
| `1PLAT_INTEGRATION.md` | Полная техническая документация |
| `ONEPLAT_DEPLOYMENT.md` | Пошаговое развёртывание |
| `INTEGRATION_SUMMARY.md` | Этот файл (краткое резюме) |

### Тестирование

| Файл | Что делает |
|------|-----------|
| `test_1plat_webhook.py` | Отправляет тестовый callback |
| `check_syntax.py` | Проверяет синтаксис Python |

---

## Где что находится

### Создание платежа
**Файл:** `handlers/subscription.py`  
**Функция:** `process_pay_1plat()`

### Получение callback'а
**Файл:** `handlers/webhooks.py`  
**Endpoint:** `POST /1plat-webhook`

### Проверка подписи
**Файл:** `services/oneplat.py`  
**Функция:** `verify_callback()`

### Активация подписки
**Файл:** `services/cryptobot.py`  
**Функция:** `process_paid_invoice()`

---

## Отладка

### Webhook не получает callback'и

**Проверить:**
1. Верифицирован ли домен в 1Plat? ✓
2. Правильный ли callback URL? ✓
3. Работает ли Nginx пробрасывание? ✓
4. Слушает ли Quart на 8080? ✓

```bash
# Проверка:
curl -X POST http://localhost:8080/1plat-webhook \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'
```

### Неверная подпись

1. Проверить `ONEPLAT_SHOP_SECRET` в `.env`
2. Убедиться, что скопирован правильно (без пробелов)
3. Посмотреть в логах что приходит в callback

### Платеж не активируется

1. Проверить статус платежа в логах
2. Убедиться, что статус = 1 или 2
3. Проверить подключение к Remnawave API

---

## Статистика кода

```
Новых файлов: 5
├── services/oneplat.py (196 строк)
├── handlers/webhooks.py (119 строк)
├── 1PLAT_INTEGRATION.md (284 строки)
├── ONEPLAT_DEPLOYMENT.md (445 строк)
└── test_1plat_webhook.py (168 строк)

Обновлённых файлов: 8
├── handlers/subscription.py (+100 строк)
├── config.py (+4 строки)
├── database.py (+20 строк)
└── остальные обновления

Строк кода добавлено: ~1200+
```

---

## Готово к production ✅

После следования инструкции ONEPLAT_DEPLOYMENT.md:

✅ Полная поддержка платежей по картам  
✅ СБП платежи  
✅ Автоматическая активация подписки  
✅ Проверка подписей (безопасность)  
✅ Webhook на HTTPS  
✅ Systemd сервис (автозагрузка)  
✅ SSL сертификат  
✅ Полная документация  

---

## Ссылки

- 📚 [Полная документация 1Plat](1PLAT_INTEGRATION.md)
- 🚀 [Инструкция развёртывания](ONEPLAT_DEPLOYMENT.md)
- 🧪 [Тестовый скрипт](test_1plat_webhook.py)
- 📝 [История изменений](CHANGES.md)
- 🔍 [Основной README](README.md)

---

## Автор

Hamzat Yaudarov

## Версия

1.0.0 - 1Plat Payment Integration

## Лицензия

MIT
