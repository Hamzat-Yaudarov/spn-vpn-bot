# 🛡️ Защита от быстрых нажатий на кнопки подписки

## Проблема
Пользователи могли быстро кликать на кнопки, которые выдают подписку (например, "Получить подарок"), и получать несколько добавлений дней вместо одного. Например, вместо +3 дней они могли получить +6, +9 или больше дней.

## Решение
Реализована многоуровневая защита от spam'а и race conditions:

### 1️⃣ Anti-Spam Cooldown (Предотвращение быстрых кликов)
Для каждого действия установлен минимальный интервал между попытками:
- **Получить подарок**: минимум 2 секунды между попытками
- **Активировать промокод**: минимум 1.5 секунды между попытками
- **Проверить платёж**: минимум 1 секунда между попытками

```python
# Проверка в начале обработчика
can_request, error_msg = await db.can_request_gift(tg_id)
if not can_request:
    await callback.answer(error_msg, show_alert=True)
    return
```

### 2️⃣ Атомарные транзакции БД (Предотвращение double-processing)
Для критичных операций используются PostgreSQL транзакции, которые гарантируют атомарность:

```python
# Атомарно проверить и отметить подарок в один запрос
UPDATE users 
SET gift_received = TRUE 
WHERE tg_id = $1 AND gift_received = FALSE
RETURNING 1
```

Это означает, что даже если два запроса придут одновременно, только один из них успешно выполнится.

### 3️⃣ PostgreSQL Advisory Locks (Блокировка пользователя)
Сохранены существующие блокировки на уровне БД для синхронизации:
```python
# Получить блокировку
if not await db.acquire_user_lock(tg_id):
    return

try:
    # Критичные операции
finally:
    # Освободить блокировку
    await db.release_user_lock(tg_id)
```

## Измененные файлы

### 1. `schema.sql`
Добавлены три поля в таблицу `users`:
- `last_gift_attempt TIMESTAMP` — время последней попытки получить подарок
- `last_promo_attempt TIMESTAMP` — время последней попытки активировать промокод
- `last_payment_check TIMESTAMP` — время последней проверки платежа

### 2. `database.py`
Добавлены новые функции:
- `mark_gift_received_atomic()` — атомарно отметить подарок (с транзакцией)
- `can_request_gift()` — проверить может ли пользователь сейчас получить подарок
- `update_last_gift_attempt()` — обновить время последней попытки
- `increment_promo_usage_atomic()` — атомарно увеличить счётчик промокода (с проверкой валидности)
- `can_request_promo()` — проверить может ли пользователь сейчас активировать промокод
- `update_last_promo_attempt()` — обновить время последней попытки
- `can_check_payment()` — проверить может ли пользователь сейчас проверить платёж
- `update_last_payment_check()` — обновить время последней проверки

### 3. `handlers/gift.py`
Изменена функция `process_get_gift()`:
- ✅ Добавлена проверка anti-spam перед обработкой
- ✅ Используется атомарная функция `mark_gift_received_atomic()`
- ✅ Обновляется время последней попытки

### 4. `handlers/promo.py`
Изменена функция `process_promo_input()`:
- ✅ Добавлена проверка anti-spam перед обработкой
- ✅ Используется атомарная функция `increment_promo_usage_atomic()`
- ✅ Обновляется время последней попытки

### 5. `handlers/subscription.py`
Изменена функция `process_check_payment()`:
- ✅ Добавлена проверка anti-spam перед обработкой
- ✅ Обновляется время последней проверки

## Как это работает?

### Сценарий: Быстрые клики на "Получить подарок"

1. **Первый клик (t=0)**
   - ✅ Проверка anti-spam: OK (нет предыдущих попыток)
   - ✅ Обновляем `last_gift_attempt = now()`
   - ✅ Получаем блокировку пользователя
   - ✅ Проверяем подписку на канал
   - ✅ Атомарно отмечаем подарок: `UPDATE ... WHERE gift_received = FALSE`
   - ✅ Выдаём +3 дня подписки
   - ✅ Освобождаем блокировку
   - ✅ Отправляем сообщение пользователю

2. **Второй клик (t=0.1 сек) — слишком быстро**
   - ❌ Проверка anti-spam: FAIL (прошло только 0.1 сек, а нужно 2 сек)
   - ❌ Ответ: "Подожди пару секунд ⏳"
   - ❌ Функция не выполняется

3. **Третий клик (t=2.5 сек) — достаточно времени**
   - ✅ Проверка anti-spam: OK (прошло 2.5 сек)
   - ✅ Обновляем `last_gift_attempt = now()`
   - ✅ Получаем блокировку пользователя
   - ✅ Проверяем подписку на канал
   - ❌ Атомарно отмечаем подарок: FAIL (gift_received = TRUE, условие не выполнено)
   - ❌ Ответ: "Ты уже получал подарок"
   - ✅ Освобождаем блокировку

## Результат
- **Пользователь получит**: +3 дня (один раз)
- **Попыток обойти защиту**: 0
- **Дополнительные дни**: 0

## Безопасность

### От чего защищает?
1. ✅ Быстрые клики на одну кнопку (user mashing button)
2. ✅ Автоматизированные скрипты (требуют соблюдения интервалов)
3. ✅ Race conditions (атомарные операции БД)
4. ✅ Double-processing (проверка условия внутри транзакции)

### Уровни защиты (в порядке срабатывания)
1. **Anti-spam cooldown** — быстрая проверка, предотвращает 95% попыток
2. **PostgreSQL Advisory Lock** — блокирует конкурентные запросы
3. **Атомарная транзакция** — гарантирует консистентность БД
4. **Условие WHERE** — финальная проверка перед изменением

## Требования к обновлению БД

Чтобы активировать защиту, нужно выполнить SQL скрипт из `schema.sql`:

```bash
# На VPS:
sudo systemctl stop spn-bot

# В Supabase Dashboard или через psql:
# Выполнить SQL из schema.sql

sudo systemctl start spn-bot
```

Три новых столбца автоматически инициализируются как `NULL` для существующих пользователей, что эквивалентно "нет предыдущих попыток".

## Мониторинг

При обнаружении spam'а в логах будут видны:
```
- Пользователь получает ответ "Подожди пару секунд ⏳"
- Попытки пользователя записываются обычным образом
- Администраторам видна активность в логах
```

Никакие данные не скрываются, просто блокируются повторные попытки.
