# This file is kept for backward compatibility but is no longer used
# The news channel button now links directly to the channel instead of using a callback

from aiogram import Router

router = Router()

# No callbacks defined - the news channel button is now a direct link
