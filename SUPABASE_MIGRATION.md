# 🚀 Миграция бота на Supabase PostgreSQL

**Время: 10 минут**

---

## 📋 Что изменилось?

- ✅ БД переехала с **SQLite на VPS** → **PostgreSQL в облаке (Supabase)**
- ✅ Все функции `database.py` теперь **асинхронные** (async/await)
- ✅ Все handlers обновлены с `await`
- ✅ Добавлена **система advisory locks** вместо file locks
- ✅ Оптимизированы **индексы** для быстрых запросов
- ✅ Добавлены **триггеры** для автоматического обновления timestamps

---

## 🎯 Чек-лист перед началом

- [ ] У вас есть credentials Supabase (SUPABASE_URL, SUPABASE_KEY, DATABASE_URL)
- [ ] Вы находитесь в корне проекта на VPS
- [ ] Вы сделали бэкап текущей БД (опционально)

---

## ⚡ ШАГ 1 - Создаём таблицы в Supabase

**Вариант A: Через веб-интерфейс Supabase**

1. Перейди на https://supabase.com/dashboard
2. Выбери свой проект
3. Нажми на "SQL Editor" в левом меню
4. Нажми "+ New Query"
5. Скопируй весь код из файла `schema.sql` (локально) и вставь в редактор
6. Нажми "Run" или Ctrl+Enter

**Вариант B: Через psql (если у тебя установлен PostgreSQL CLI)**

```bash
# На твоём ПК (не на VPS!)
psql -h rpzupbtpfcqnwlxzhndd.supabase.co \
     -U postgres \
     -d postgres \
     -f schema.sql
# Введи пароль из DATABASE_URL
```

---

## 🔑 ШАГ 2 - Обновляем .env на VPS

Подключись к VPS:

```bash
ssh root@ВАШ_IP
```

Отредактируй `.env` файл:

```bash
nano /root/spn-vpn-bot/.env
```

**Замени эти строки:**

```env
# Удали эти строки:
# DB_FILE=spn_vpn_bot.db

# Добавь эти строки:
SUPABASE_URL=https://rpzupbtpfcqnwlxzhndd.supabase.co
SUPABASE_KEY=sb_publishable_rAPEhWLXaexhMaKBbOvg-A_Xo1tz12I
DATABASE_URL=postgresql://postgres:PASSWORD@db.rpzupbtpfcqnwlxzhndd.supabase.co:5432/postgres
```

**Важно:** 
- Замени PASSWORD на реальный пароль из твоего DATABASE_URL
- DATABASE_URL должен быть полным (с портом :5432)

Сохрани (Ctrl+O, Enter, Ctrl+X)

---

## 📦 ШАГ 3 - Обновляем код на VPS

```bash
# Перейди в папку проекта
cd /root/spn-vpn-bot

# Обновляем код из GitHub (если используешь git)
git pull origin main

# Или если нет git - обновляешь файлы вручную через SCP

# Установим новый пакет asyncpg
source venv/bin/activate
pip install -r requirements.txt --upgrade
```

---

## 🧪 ШАГ 4 - Тестируем подключение

```bash
# Активируем виртуальное окружение
source /root/spn-vpn-bot/venv/bin/activate

# Запускаем бота (для теста)
python3 /root/spn-vpn-bot/main.py
```

**Если видишь в логах без красных ошибок** ✅ - подключение работает!

Нажми Ctrl+C чтобы остановить.

---

## 🚀 ШАГ 5 - Перезагружаем бота

```bash
sudo systemctl restart spn-bot
```

Проверяем статус:

```bash
sudo systemctl status spn-bot
```

Должно быть: `Active: active (running)`

---

## 📊 ШАГ 6 - Проверяем логи

```bash
sudo journalctl -u spn-bot -f
```

Ищем в логах:
- ✅ `Database pool initialized successfully` - БД подключилась
- ✅ `Bot started polling...` - бот запустился
- ❌ Красные ошибки - есть проблемы

---

## 🔍 Если что-то не работает

### Ошибка: "Connection refused" или "Database connection failed"

1. **Проверь .env файл:**
   ```bash
   cat /root/spn-vpn-bot/.env | grep DATABASE_URL
   ```

2. **Проверь что Supabase проект работает:**
   - Перейди на https://supabase.com/dashboard
   - Выбери проект
   - Убедись что статус зелёный

3. **Проверь пароль в DATABASE_URL:**
   - В URL должен быть реальный пароль
   - Не должно быть символов `<` и `>`
   - Сравни с тем что ты видишь в Supabase dashboard

4. **Если IP VPS заблокирован:**
   - Перейди в Supabase → Project Settings → Database
   - Нажми "Add IPv4 address" и укажи IP своего VPS
   - Или выбери "Allow all connections" (менее безопасно)

### Ошибка: "Table doesn't exist"

Убедись что ты выполнил ШАГ 1 - создание таблиц в Supabase.

### Бот работает но не отвечает в Telegram

1. Проверь логи:
   ```bash
   sudo journalctl -u spn-bot -n 50
   ```

2. Перезагрузи бота:
   ```bash
   sudo systemctl restart spn-bot
   ```

---

## 💾 Что произойдёт со старой БД?

На VPS останется старый файл `spn_vpn_bot.db`, но бот будет использовать Supabase.

Если хочешь удалить старый файл:

```bash
# Бэкап (на случай если что-то пойдёт не так)
cp /root/spn-vpn-bot/spn_vpn_bot.db /root/spn-vpn-bot/spn_vpn_bot.db.backup

# Удаление
rm /root/spn-vpn-bot/spn_vpn_bot.db
```

---

## 🎯 Проверь что всё работает

Проверь что:

1. ✅ `sudo systemctl status spn-bot` показывает `active (running)`
2. ✅ Бот отвечает на `/start` в Telegram
3. ✅ Можешь просмотреть пользователей в Supabase dashboard
4. ✅ Нет ошибок в логах: `sudo journalctl -u spn-bot -f`

---

## 📈 Преимущества Supabase

- ✅ **Автоматические бэкапы** - данные в безопасности
- ✅ **Real-time** - можно добавить live уведомления
- ✅ **Admin Dashboard** - смотреть БД прямо в веб-интерфейсе
- ✅ **Масштабируемость** - работает с миллионами запросов
- ✅ **Security** - enterprise-уровень защиты
- ✅ **Отделение** - БД отдельно от приложения

---

## ❓ Вопросы?

Проверь:
- Логи: `sudo journalctl -u spn-bot -f`
- DATABASE_URL: `cat /root/spn-vpn-bot/.env`
- Статус БД: https://supabase.com/dashboard

Всё работает? **Поздравляем!** 🎉
