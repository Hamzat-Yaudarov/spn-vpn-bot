# 🚀 Как запустить бот на VPS 24/7

**Время: 15 минут | Сложность: Очень легко**

---

## 📋 ЧТО ВАМ НУЖНО

1. **VPS сервер** (Ubuntu 20.04+)
   - Подойдут: Hetzner, DigitalOcean, Linode, Beget, и т.д.
   - Минимум: 1 ГБ RAM, 1 ядро CPU
   
2. **SSH доступ** (обычно есть в письме от хостера)
   - IP адрес сервера
   - Пароль или SSH ключ

3. **Ваши переменные окружения**:
   - BOT_TOKEN
   - ADMIN_ID
   - Все остальные из `config.py`

---

## ⚡ БЫСТРЫЙ СТАРТ (скопируй и вставляй в терминал)

### Шаг 1️⃣ - Подключись к серверу

```bash
ssh root@ВАШ_IP_АДРЕС
# Или если используется ключ:
ssh -i /path/to/key root@ВАШ_IP_АДРЕС
```

Введи пароль если потребуется.

---

### Шаг 2️⃣ - Скачай код проекта

**Способ A: Если у вас есть GitHub репозиторий**
```bash
cd /root && git clone https://github.com/YOUR_USERNAME/spn-vpn-bot.git && cd spn-vpn-bot
```

**Способ B: Если нет репо (просто скопируй файлы)**
```bash
mkdir -p /root/spn-vpn-bot && cd /root/spn-vpn-bot
# Затем скопируй все файлы проекта через SCP или другой способ
```

---

### Шаг 3️⃣ - Создай .env файл

```bash
cat > .env << 'EOF'
BOT_TOKEN=тут_ваш_токен
ADMIN_ID=тут_ваш_admin_id
SUPPORT_URL=https://t.me/yourname
NEWS_CHANNEL_USERNAME=ваш_канал
TELEGRAPH_AGREEMENT_URL=ссылка_на_условия
REMNAWAVE_BASE_URL=https://spn.idlebat.online/api
REMNAWAVE_API_TOKEN=ваш_токен
DEFAULT_SQUAD_UUID=ваш_squad_uuid
CRYPTOBOT_TOKEN=ваш_токен
CRYPTOBOT_API_URL=https://pay.crypt.bot/api
DB_FILE=spn_vpn_bot.db
LOG_LEVEL=INFO
EOF
```

**Замени значения на СВОИ!** (из вашего локального .env)

Проверь что всё правильно:
```bash
cat .env
```

---

### Шаг 4️⃣ - Установи зависимости

```bash
# Обновляем пакеты системы
apt update && apt install -y python3 python3-pip python3-venv

# Переходим в папку проекта
cd /root/spn-vpn-bot

# Создаём виртуальное окружение
python3 -m venv venv

# Активируем его
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

---

### Шаг 5️⃣ - Тестируем что бот работает

```bash
source venv/bin/activate
python3 main.py
```

**Если видишь в логах без красных ошибок** - отлично! ✅

Нажми **Ctrl+C** чтобы остановить.

---

### Шаг 6️⃣ - Настраиваем АВТОЗАПУСК (самое важное!)

Создаём systemd сервис - это даст нам:
- ✅ Бот запускается автоматически при перезагрузке сервера
- ✅ Если бот упадёт - он перезагрузится сам
- ✅ Логи сохраняются и доступны в любое время

**Создай файл сервиса:**

```bash
sudo tee /etc/systemd/system/spn-bot.service > /dev/null << 'EOF'
[Unit]
Description=SPN VPN Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/spn-vpn-bot
Environment="PATH=/root/spn-vpn-bot/venv/bin"
ExecStart=/root/spn-vpn-bot/venv/bin/python3 /root/spn-vpn-bot/main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF
```

**Активируем сервис:**

```bash
sudo systemctl daemon-reload
sudo systemctl enable spn-bot
sudo systemctl start spn-bot
```

**Проверяем что работает:**

```bash
sudo systemctl status spn-bot
```

Должно быть: `● spn-bot.service - SPN VPN Bot ... Active: active (running)`

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ (держи под рукой)

```bash
# 📈 Статус бота
sudo systemctl status spn-bot

# ▶️ Запустить бота
sudo systemctl start spn-bot

# ⏹️ Остановить бота
sudo systemctl stop spn-bot

# 🔄 Перезагрузить бота
sudo systemctl restart spn-bot

# 📜 Смотреть логи (последние 50 строк)
sudo journalctl -u spn-bot -n 50

# 📺 Смотреть логи в реальном времени (как tail -f)
sudo journalctl -u spn-bot -f

# 🔍 Искать ошибки в логах
sudo journalctl -u spn-bot | grep -i error

# 📁 Проверить размер БД
du -h /root/spn-vpn-bot/spn_vpn_bot.db
```

---

## 🐛 ЧТО ДЕЛАТЬ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### ❌ "Connection refused" или бот не запускается

```bash
# 1. Проверь что .env на месте
ls -la /root/spn-vpn-bot/.env

# 2. Проверь что там правильные значения
cat /root/spn-vpn-bot/.env

# 3. Посмотри ошибку в логах
sudo journalctl -u spn-bot -n 100
```

### ❌ Бот запустился но не отвечает в Telegram

1. **Проверь что бот запущен:**
   ```bash
   sudo systemctl status spn-bot
   ```

2. **Проверь логи:**
   ```bash
   sudo journalctl -u spn-bot -f
   ```

3. **Убедись что у тебя правильный BOT_TOKEN в .env**

4. **Перезагрузи бота:**
   ```bash
   sudo systemctl restart spn-bot
   ```

### ❌ Python модули не установлены

```bash
source /root/spn-vpn-bot/venv/bin/activate
pip install -r /root/spn-vpn-bot/requirements.txt
sudo systemctl restart spn-bot
```

---

## 🔄 ОБНОВЛЕНИЕ КОДА

Если ты обновил код в GitHub:

```bash
cd /root/spn-vpn-bot
git pull origin main
sudo systemctl restart spn-bot
```

---

## 💾 РЕЗЕРВНАЯ КОПИЯ БД

Твоя база данных важна! Давай сделаем резервную копию:

```bash
# Создаём папку для бэкапов
mkdir -p /root/spn-vpn-bot/backups

# Копируем БД с временной меткой
cp /root/spn-vpn-bot/spn_vpn_bot.db /root/spn-vpn-bot/backups/spn_vpn_bot.db.$(date +%Y%m%d_%H%M%S)

# Проверяем бэкапы
ls -lah /root/spn-vpn-bot/backups/
```

---

## 📡 МОНИТОРИНГ (опционально)

Если хочешь знать сколько оперативной памяти использует бот:

```bash
# Использование памяти и CPU
ps aux | grep "[p]ython3 main.py"

# Всё что используется сервером
free -h
df -h
```

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

Всё настроено правильно если:

1. ✅ `sudo systemctl status spn-bot` показывает `active (running)`
2. ✅ Бот отвечает на команды в Telegram
3. ✅ `sudo journalctl -u spn-bot -f` показывает логи без красных ошибок
4. ✅ Бот остаётся включён после перезагрузки сервера

---

## 🎉 ВОТ И ВСЁ!

Теперь твой бот работает **24/7** на VPS! 🚀

Когда закроешь ноутбук - бот будет работать дальше.
Когда сервер перезагрузится - бот запустится сам.

**Вопросы?** Смотри логи командой `sudo journalctl -u spn-bot -f`

---

## 🆘 КОНТРОЛЬНЫЙ СПИСОК

Перед тем как уходить, проверь:

- [ ] SSH доступ работает
- [ ] .env файл создан и заполнен правильно
- [ ] `sudo systemctl status spn-bot` показывает `active (running)`
- [ ] Бот отвечает в Telegram
- [ ] Можешь выполнить команду `sudo journalctl -u spn-bot -f`
- [ ] Знаешь как перезагрузить бота: `sudo systemctl restart spn-bot`
