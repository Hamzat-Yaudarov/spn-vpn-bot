# ✅ Чек-лист перед развёртыванием

## Перед развёртыванием на VPS

### 📋 Код и файлы
- [ ] Все изменения закоммичены в git
- [ ] Нет синтаксических ошибок (проверено через импорты)
- [ ] Все новые файлы созданы:
  - [ ] `services/xui.py`
  - [ ] `LARGE_UPDATE_SUMMARY.md`
  - [ ] `DEPLOY_DUAL_SUBSCRIPTION.md`
  - [ ] `CHANGES_DETAILED.md`
  - [ ] `CHECKLIST_BEFORE_DEPLOY.md` (этот файл)

### 🔧 Конфигурация
- [ ] Проверены параметры 3X-UI в `config.py`:
  ```
  XUI_PANEL_URL = "https://51.250.117.234:2053"
  XUI_PANEL_PATH = "/sXvL8myMex46uSa3NP/panel"
  XUI_USERNAME = "U0UiUl76S0"
  XUI_PASSWORD = "2W1SwoZ0Ix"
  SUB_PORT = 2096
  SUB_EXTERNAL_HOST = "51.250.117.234"
  INBOUND_ID = 1
  ```
- [ ] Проверено что `DATABASE_URL` правильный
- [ ] Проверено что `BOT_TOKEN` правильный
- [ ] Проверено что `REMNAWAVE_BASE_URL`, `REMNAWAVE_API_TOKEN`, `DEFAULT_SQUAD_UUID` правильные

### 💾 База данных
- [ ] Резервная копия БД сделана (опционально но рекомендуется)
- [ ] Убедитесь что БД доступна и работает
- [ ] Проверьте что текущих пользователей не менее 1 (для теста)

### 🌐 Доступность
- [ ] Проверен доступ до 3X-UI панели (51.250.117.234:2053)
- [ ] Проверены учётные данные 3X-UI (можно с помощью curl)
- [ ] Проверен доступ до Remnawave API
- [ ] Проверен доступ до CryptoBot и Yookassa (если используются)

### 📊 Тестирование на локальной машине (опционально)
- [ ] Основной процесс оплаты работает (regular)
- [ ] Выбор типа подписки работает
- [ ] Разные тарифы показываются для разных типов
- [ ] Нет ошибок импорта

---

## Развёртывание на VPS

### 1️⃣ Подготовка
```bash
# Остановить бота
sudo systemctl stop spn-bot

# Перейти в директорию проекта
cd /path/to/spn-vpn-bot

# Сделать резервную копию
git stash  # если есть локальные изменения
```

### 2️⃣ Обновление кода
```bash
# Обновить код с git
git pull origin main

# Или если не используется git:
# - вручную скопировать все файлы
# - убедиться что новые файлы скопированы
```

### 3️⃣ Проверка зависимостей
```bash
# Установить зависимости (на случай если есть новые)
pip install -r requirements.txt

# Проверить что asyncpg и aiohttp установлены
pip list | grep -E "asyncpg|aiohttp"
```

### 4️⃣ Проверка перед запуском
```bash
# Проверить синтаксис основных файлов
python3 -c "import config; print('config OK')"
python3 -c "import database; print('database OK')"
python3 -c "import services.xui; print('xui OK')"
python3 -c "import handlers.subscription; print('subscription OK')"
```

### 5️⃣ Запуск бота
```bash
# Запустить бота
sudo systemctl start spn-bot

# Или если вручную:
python main.py
```

### 6️⃣ Проверка логов
```bash
# Смотрите логи миграции БД
sudo journalctl -u spn-bot -f --lines=50

# Должны увидеть:
# ✨ ВСЕ МИГРАЦИИ УСПЕШНО ЗАВЕРШЕНЫ ✨
# Database pool initialized successfully
# Dispatcher started
```

---

## После развёртывания

### 🧪 Функциональное тестирование

#### Тест 1: Выбор типа подписки
- [ ] Откройте чат с ботом
- [ ] Нажмите "Оформить подписку"
- [ ] Должны видеть 2 варианта:
  - [ ] 🔐 Обычная подписка
  - [ ] 🛡️ Обычная подписка + Обход глушилок

#### Тест 2: Разные тарифы
- [ ] Выберите "Обычная подписка"
- [ ] Проверьте тарифы: 100, 249, 449, 990 ✅
- [ ] Вернитесь назад
- [ ] Выберите "Обычная подписка + Обход глушилок"
- [ ] Проверьте тарифы: 150, 349, 599, 1190 ✅

#### Тест 3: Создание Remnawave подписки
- [ ] Выберите regular подписку
- [ ] Выберите 1 месяц (100₽)
- [ ] Выберите CryptoBot
- [ ] Смотрите логи
- [ ] Должны видеть в логах: "Created CryptoBot invoice"
- [ ] **НЕ платите**, просто проверьте что счёт создался

#### Тест 4: Проверка 3X-UI подписки (опционально - рискованно платить)
- [ ] Если готовы платить реально:
  - [ ] Выберите anti_jamming подписку
  - [ ] Выберите 1 месяц (150₽)
  - [ ] Выберите CryptoBot
  - [ ] Оплатите счёт
  - [ ] Проверьте логи на:
    - [ ] "Created CryptoBot invoice"
    - [ ] "Processed payment for user"
    - [ ] "Created 3X-UI client for user"
  - [ ] Проверьте что пользователю отправлены ДВЕ ссылки

#### Тест 5: Просмотр подписки (my_subscription)
- [ ] Нажмите "Моя подписка"
- [ ] Если у вас есть regular подписка → 1 ссылка (Remnawave)
- [ ] Если у вас есть anti_jamming подписка → 2 ссылки (Remnawave + 3X-UI)

#### Тест 6: Работа старых функций
- [ ] Промокоды работают (handlers/promo.py)
- [ ] Подарок работает (handlers/gift.py)
- [ ] Реферальная программа работает (handlers/referral.py)

### 📊 Проверка БД

```bash
# Подключиться к БД и проверить новые столбцы
psql $DATABASE_URL

# Показать структуру таблицы users
\d users

# Должны быть новые столбцы:
# subscription_type TEXT
# balance NUMERIC
# xui_uuid TEXT
# xui_username TEXT
# xui_subscription_until TIMESTAMP

# Проверить что данные целые
SELECT tg_id, subscription_type, balance FROM users LIMIT 5;
```

### 📋 Мониторинг после развёртывания

```bash
# Проверяйте логи на предмет ошибок 3X-UI:
sudo journalctl -u spn-bot -f | grep -i "3x-ui\|xui\|error"

# Ищите эти сообщения:
# ✅ 3X-UI сессия успешно открыта
# ✅ Клиент 3X-UI создан для пользователя
```

---

## Откат (если что-то пошло не так)

### Если бот не запускается

```bash
# Проверить синтаксис
python3 -m py_compile config.py database.py

# Посмотреть полный лог ошибки
journalctl -u spn-bot --lines=100
```

### Если нужно вернуться на старую версию

```bash
# Откатить последний коммит
git revert HEAD

# Или просто вернуться на старую ветку
git checkout main~1

# Перезапустить
sudo systemctl restart spn-bot
```

### Если проблема с 3X-UI

1. Проверьте параметры конфигурации в `config.py`
2. Проверьте доступ до панели: `curl https://51.250.117.234:2053/...`
3. Проверьте логины/пароли
4. Если не критично - закомментируйте создание 3X-UI клиентов (временно)

### Если проблема с БД миграцией

```bash
# Миграции идемпотентны, можно просто перезапустить бот
sudo systemctl restart spn-bot

# Если не помогло - проверьте логи:
sudo journalctl -u spn-bot -f | grep -i "migration\|alter\|error"
```

---

## Финальная проверка готовности к production

### 📈 Перед полным запуском:
- [ ] Все чеклисты выше пройдены ✅
- [ ] Нет ошибок в логах
- [ ] Основные функции работают
- [ ] Резервная копия БД сделана
- [ ] Есть способ быстро откатиться (git или файлы)

### 🚀 После запуска:
- [ ] Мониторите логи в течение 1 часа
- [ ] Проверьте что первые платежи обрабатываются правильно
- [ ] Убедитесь что 3X-UI клиенты создаются для anti_jamming
- [ ] Ждите обратной связи от пользователей

---

## Контактная информация для помощи

Если что-то не работает:
1. Проверьте логи: `sudo journalctl -u spn-bot -f`
2. Посмотрите документацию: `LARGE_UPDATE_SUMMARY.md`
3. Проверьте параметры: `DEPLOY_DUAL_SUBSCRIPTION.md`
4. Посмотрите что изменилось: `CHANGES_DETAILED.md`
