# 📝 ИЗМЕНЕНИЯ ПРОЕКТА

## 🎯 Последнее обновление: Интеграция 1Plat Payment (картамы/СБП)

### Новая функциональность

Вместо заглушки Yookassa реализована полная интеграция платежной системы **1Plat**:

- 💳 Оплата банковскими картами (VISA, MasterCard, МИР)
- 📱 Оплата через СБП (система быстрых платежей по телефону)
- ✅ Автоматическая активация подписки только после callback'а от 1Plat
- 🔐 Проверка подписей callback'ов (HMAC-SHA256 и MD5)
- 🌐 Webhook сервер на Quart для получения callback'ов

---

## 📝 ИЗМЕНЕНИЯ ПРОЕКТА - SQLite → Supabase PostgreSQL

---

## 🎯 Интеграция 1Plat - Измененные файлы

### 1. **services/oneplat.py** (✨ НОВЫЙ FILE)

Основной сервис для работы с API 1Plat:

```python
async def create_oneplat_payment(
    amount: int,
    tariff_code: str,
    tg_id: int,
    method: str = "card"
) -> Optional[Dict[str, Any]]:
    """Создать платеж через API 1Plat"""

async def get_payment_info(guid: str) -> Optional[Dict[str, Any]]:
    """Получить информацию о платеже"""

def verify_callback(body: Dict[str, Any]) -> bool:
    """Проверить подпись callback'а (поддерживает обе подписи)"""

def verify_callback_signature(body, signature, use_v2=False) -> bool:
    """Проверить одну подпись (v1=HMAC-SHA256 или v2=MD5)"""
```

**Особенности:**
- ✅ Поддержка обоих методов подписи (signature и signature_v2)
- ✅ Создание платежей с методами card и sbp
- ✅ Проверка статусов платежей
- ✅ Подробное логирование ошибок

---

### 2. **handlers/webhooks.py** (✨ НОВЫЙ FILE)

Quart blueprint для обработки webhook'ов от 1Plat:

```python
@webhook_bp.route('/1plat-webhook', methods=['POST'])
async def handle_oneplat_webhook():
    """Обработка callback'ов от 1Plat"""
```

**Логика:**
1. Получает callback от 1Plat
2. Проверяет подпись (HMAC-SHA256 или MD5)
3. Проверяет статус платежа
4. Только если статус = 1 или 2 (оплачено):
   - Активирует подписку в Remnawave
   - Обрабатывает реферальную программу
   - Отправляет сообщение пользователю

**Статусы платежей:**
- -2: Ошибка
- -1: Черновик
- 0: Ожидает оплаты
- 1: Оплачен (ожидает подтверждения)
- 2: Подтвержден ✅

---

### 3. **config.py** (✏️ ОБНОВЛЕН)

Добавлены новые переменные:

```python
ONEPLAT_SHOP_ID = int(os.getenv("ONEPLAT_SHOP_ID", "0"))
ONEPLAT_SHOP_SECRET = os.getenv("ONEPLAT_SHOP_SECRET", "")
ONEPLAT_BASE_URL = os.getenv("ONEPLAT_BASE_URL", "https://1plat.cash")
ONEPLAT_CALLBACK_URL = os.getenv("ONEPLAT_CALLBACK_URL", "")
```

**Где получить:**
- ID магазина и Secret ключ - в админ-панели 1Plat ЛК
- CALLBACK_URL - `https://spn.bot.idlebat.online/1plat-webhook`

---

### 4. **database.py** (✏️ ОБНОВЛЕН)

Добавлены миграция и функции для 1Plat:

**Новая миграция:**
```sql
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_guid TEXT;
ALTER TABLE payments ADD COLUMN IF NOT EXISTS payment_method TEXT;
```

**Новые функции:**
```python
async def create_oneplat_payment(...)
async def get_payment_by_guid(payment_guid)
async def get_pending_oneplat_payments()
async def update_payment_status_by_guid(payment_guid, status)
```

**Обновленная функция:**
```python
# Теперь работает с обоими провайдерами (CryptoBot и 1Plat)
async def update_payment_status_by_invoice(invoice_id, status):
    # Обновляет как invoice_id (CryptoBot), так и payment_guid (1Plat)
```

---

### 5. **handlers/subscription.py** (✏️ ОБНОВЛЕН)

Заменена заглушка Yookassa на полную интеграцию 1Plat:

**Было:**
```python
@router.callback_query(F.data == "pay_yookassa")
async def process_pay_yookassa(callback: CallbackQuery, state: FSMContext):
    """Заглушка для оплаты через Yookassa"""
    # Показывает сообщение об ошибке
```

**Стало:**
```python
@router.callback_query(F.data == "pay_yookassa")
async def process_pay_yookassa(callback: CallbackQuery, state: FSMContext):
    """Показать выбор метода оплаты для 1Plat"""
    # Предлагает карту или СБП

@router.callback_query(F.data.startswith("pay_1plat_"))
async def process_pay_1plat(callback: CallbackQuery, state: FSMContext):
    """Создать платеж через 1Plat"""
    # Создаёт платеж в API 1Plat
    # Показывает реквизиты пользователю
    # Сохраняет в БД

@router.callback_query(F.data == "check_oneplat_payment")
async def process_check_oneplat_payment(callback: CallbackQuery):
    """Проверить статус платежа 1Plat"""
    # Позволяет пользователю вручную проверить платеж
```

---

### 6. **states.py** (✏️ ОБНОВЛЕН)

Добавлено новое состояние:

```python
class UserStates(StatesGroup):
    choosing_1plat_method = State()  # Выбор способа оплаты (карта/СБП)
```

---

### 7. **main.py** (✏️ ОБНОВЛЕН)

Добавлена интеграция Quart для webhook сервера:

```python
from quart import Quart
from handlers.webhooks import webhook_bp, set_bot

# Инициализируем Quart приложение
app = Quart(__name__)
app.register_blueprint(webhook_bp)
set_bot(bot)

async def run_webhook_server():
    """Запустить Quart сервер на порту 8080"""
    await app.run_task(host="0.0.0.0", port=8080)

async def main():
    # Запускаем bot polling и webhook сервер одновременно
    await asyncio.gather(
        run_bot(),
        run_webhook_server()
    )
```

**Результат:**
- Bot polling работает как обычно
- Quart сервер слушает на порте 8080
- Может получать callback'и от 1Plat

---

### 8. **services/cryptobot.py** (✏️ ОБНОВЛЕН)

Обновлена функция `process_paid_invoice`:

```python
async def process_paid_invoice(bot, tg_id: int, invoice_id: str, tariff_code: str) -> bool:
    """
    Обработать оплаченный счёт (CryptoBot или 1Plat)

    invoice_id может быть как:
    - invoice_id из CryptoBot
    - payment_guid из 1Plat
    """
```

Теперь работает с обоими провайдерами!

---

### 9. **.env.example** (✏️ ОБНОВЛЕН)

Добавлены примеры переменных для 1Plat:

```env
ONEPLAT_SHOP_ID=12345
ONEPLAT_SHOP_SECRET=your_shop_secret_key
ONEPLAT_BASE_URL=https://1plat.cash
ONEPLAT_CALLBACK_URL=https://spn.bot.idlebat.online/1plat-webhook
```

---

### 10. **requirements.txt** (✏️ ОБНОВЛЕН)

Добавлена новая зависимость:

```diff
  aiogram>=3.0.0
  aiohttp>=3.8.5
  python-dotenv>=1.0.0
  asyncpg>=0.28.0
+ quart>=0.18.0
```

`quart` - асинхронный веб-фреймворк для webhook'ов

---

## ✨ НОВЫЕ ФАЙЛЫ

### 1. **1PLAT_INTEGRATION.md** (✨ НОВЫЙ)

Полная документация по интеграции 1Plat:
- Как это работает
- Статусы платежей
- Настройка переменных окружения
- Верификация домена (3 способа)
- Архитектура интеграции (диаграмма)
- Проверка подписей (HMAC-SHA256 и MD5)
- Тестирование с ngrok
- API endpoints 1Plat
- Решение проблем

---

### 2. **ONEPLAT_DEPLOYMENT.md** (✨ НОВЫЙ)

Пошаговая инструкция по развёртыванию на новом сервере:
- Требования и подготовка
- Клонирование репозитория
- Виртуальное окружение
- Установка зависимостей
- Настройка .env
- Проверка БД
- Запуск бота
- Настройка 1Plat в ЛК
- Настройка Nginx для webhook'ов
- SSL сертификат Let's Encrypt
- Systemd сервис для автозагрузки
- Проверка работоспособности
- Мониторинг и решение проблем
- Откат на случай проблем

---

### 3. **test_1plat_webhook.py** (✨ НОВЫЙ)

Скрипт для локального тестирования webhook'а:

```bash
# Использование:
python test_1plat_webhook.py 0      # Платеж ожидает
python test_1plat_webhook.py 1      # Платеж оплачен
python test_1plat_webhook.py 2      # Платеж подтвержден
```

Генерирует правильные подписи и отправляет на localhost:8080

---

### 4. **1plat.txt** (✨ НОВЫЙ)

Файл для верификации домена через метод 2:
```
YOUR_SHOP_ID
```

Должен быть доступен по: `https://spn.bot.idlebat.online/1plat.txt`

---

## 📊 АРХИТЕКТУРА ПЛАТЕЖЕЙ

### До (только CryptoBot)
```
Пользователь → Выбирает CryptoBot → Платит в CryptoBot → Подписка активируется
```

### Теперь (CryptoBot + 1Plat)
```
Пользователь
    ↓
Выбирает метод оплаты:
    ├→ CryptoBot (USDT, TON, BTC)
    └→ 1Plat
        ├→ Карта (VISA, MasterCard, МИР)
        └→ СБП (по телефону)
    ↓
Оплачивает
    ↓
Webhook от провайдера
    ↓
Проверка подписи
    ↓
Активирует подписку в Remnawave
    ↓
Обрабатывает рефералов
    ↓
Отправляет ссылку пользователю ✅
```

---

## 🔐 БЕЗОПАСНОСТЬ 1PLAT

### Верификация подписей

**Поддерживаются 2 метода:**

1. **HMAC-SHA256** (signature)
```python
signature = HMAC-SHA256(JSON.stringify(payload), shopSecret)
```

2. **MD5** (signature_v2)
```python
signature_v2 = MD5(merchantId + amount + shopId + shopSecret)
```

**Реализация:**
```python
def verify_callback(body: Dict[str, Any]) -> bool:
    v1_valid = verify_callback_signature(body, body.get("signature"), use_v2=False)
    v2_valid = verify_callback_signature(body, body.get("signature_v2"), use_v2=True)
    return v1_valid or v2_valid  # Валидна если хотя бы одна пройдёт
```

### Защита от race conditions

Используется блокировка пользователя при обработке платежа:
```python
if not await db.acquire_user_lock(tg_id):
    return  # Подождите, идёт обработка другого платежа

try:
    # Обрабатываем платёж
    success = await process_paid_invoice(bot, tg_id, ...)
finally:
    await db.release_user_lock(tg_id)
```

**Результат:** Одна подписка не может быть активирована дважды за раз.

---

## 🚀 МАСШТАБИРУЕМОСТЬ

### Преимущества Quart + asyncpg

1. **Асинхронность:** Webhook не блокирует polling бота
2. **Пул подключений:** 5-20 одновременных БД соединений
3. **Advisory locks:** Встроены в PostgreSQL
4. **Масштабируемость:** Можно запустить несколько инстансов бота

### Webhook порт

- **8080** - используется для webhook'ов 1Plat
- Nginx пробрасывает `/1plat-webhook` на этот порт
- HTTPS через reverse proxy Nginx

---

## 📋 ЧЕК-ЛИСТ РАЗВЁРТЫВАНИЯ

### На локальной машине (✅ готово)
- ✅ Создан сервис 1Plat (`services/oneplat.py`)
- ✅ Создан webhook обработчик (`handlers/webhooks.py`)
- ✅ Обновлена БД миграция
- ✅ Обновлены обработчики платежей
- ✅ Обновлена конфигурация
- ✅ Добавлены новые зависимости
- ✅ Написана документация
- ✅ Создан тестовый скрипт

### На новом сервере (нужно сделать)
- ⏳ SSH подключение: `ssh root@77.233.214.150`
- ⏳ Клонирование: `git clone https://github.com/Hamzat-Yaudarov/spn-vpn-bot.git`
- ⏳ Следовать ONEPLAT_DEPLOYMENT.md
- ⏳ Получить SHOP_ID и SHOP_SECRET от 1Plat
- ⏳ Установить ONEPLAT_* переменные в .env
- ⏳ Верифицировать домен в 1Plat
- ⏳ Запустить бот
- ⏳ Тестировать платежи

---

## 🎯 РЕЗУЛЬТАТ

После этого обновления:

✅ **Оплата по картам** - Пользователи могут оплачивать картами (VISA, MasterCard, МИР)
✅ **СБП** - Оплата через систему быстрых платежей по номеру телефона
✅ **Безопасность** - Проверка подписей callback'ов
✅ **Надёжность** - Активация подписки только после callback'а от 1Plat
✅ **Масштабируемость** - Асинхронная архитектура с webhook'ами
✅ **Полная документация** - Готово к production

---

## 🔄 ИЗМЕНЕННЫЕ ФАЙЛЫ

### 1. **config.py** (✏️ обновлен)
```python
# Было:
DB_FILE = os.getenv("DB_FILE", "spn_vpn_bot.db")

# Стало:
DATABASE_URL = os.getenv("DATABASE_URL", "")
SUPABASE_URL = os.getenv("SUPABASE_URL", "")
SUPABASE_KEY = os.getenv("SUPABASE_KEY", "")
```

**Что изменилось:**
- ✅ Добавлены 3 переменные для Supabase/PostgreSQL
- ✅ Удалена переменная `DB_FILE` (больше не нужна)
- ✅ Все переменные загружаются из `.env`

---

### 2. **requirements.txt** (✏️ обновлен)
```diff
  aiogram>=3.0.0
  aiohttp>=3.8.5
  python-dotenv>=1.0.0
+ asyncpg>=0.28.0
```

**Что изменилось:**
- ✅ Добавлена зависимость `asyncpg` (асинхронный драйвер PostgreSQL)
- ✅ Это нужно для асинхронной работы с PostgreSQL

---

### 3. **database.py** (🔄 полностью переписан)

**Было:**
- SQLite с синхронными операциями
- File-based блокировки (`action_lock = 1/0`)
- Прямые запросы к файлу БД

**Стало:**
- PostgreSQL с асинхронными операциями (async/await)
- Пул подключений (5-20 активных соединений)
- Advisory locks в PostgreSQL
- Все функции с `async def`

**Новые функции:**
```python
async def init_db()          # Инициализирует пул
async def close_db()         # Закрывает пул
async def get_pool()         # Возвращает пул

# Все остальные функции остались с теми же именами,
# но теперь асинхронные:
async def create_user(...)
async def get_user(...)
async def update_subscription(...)
# и т.д. - всего 30+ функций
```

**Блокировки пользователей:**
```python
# Было:
UPDATE users SET action_lock = 1 WHERE tg_id = ? AND action_lock = 0

# Стало (advisory locks):
SELECT pg_try_advisory_lock($1)  # Встроено в PostgreSQL
```

---

### 4. **main.py** (✏️ обновлен)

```python
# Было:
db.init_db()  # Синхронно

# Стало:
await db.init_db()  # Асинхронно

# Добавлено закрытие пула:
finally:
    await db.close_db()
```

---

### 5-11. **handlers/*.py** (✏️ обновлены)

Все файлы в папке `handlers/`:
- `start.py`
- `callbacks.py`
- `subscription.py`
- `gift.py`
- `referral.py`
- `promo.py`
- `admin.py`

**Что изменилось:**
Все вызовы функций `db.*()` обновлены с `await`:

```python
# Было:
db.create_user(tg_id, username)
db.update_subscription(...)
db.get_user(tg_id)

# Стало:
await db.create_user(tg_id, username)
await db.update_subscription(...)
user = await db.get_user(tg_id)
```

**Обработка результатов:**
```python
# Было (кортежи):
user = await db.get_user(tg_id)
uuid = user[3]  # remnawave_uuid

# Стало (словари):
user = await db.get_user(tg_id)
uuid = user['remnawave_uuid']  # Понятнее!
```

---

### 12. **services/cryptobot.py** (✏️ обновлен)

```python
# Было:
payment = db.get_last_pending_payment(tg_id)
for payment_id, tg_id, invoice_id, tariff_code in pending:

# Стало:
payment = await db.get_last_pending_payment(tg_id)
for payment_record in pending:
    payment_id = payment_record['id']
    tg_id = payment_record['tg_id']
    # и т.д.
```

---

## ✨ НОВЫЕ ФАЙЛЫ

### 1. **schema.sql** (✨ новый)
SQL скрипт для создания всех таблиц в Supabase:
- `users` - пользователи с полями для Supabase
- `payments` - платежи
- `promo_codes` - промокоды

**Включает:**
- ✅ Индексы для ускорения запросов
- ✅ Foreign keys для целостности данных
- ✅ Уникальные ограничения
- ✅ Триггеры для `updated_at`

---

### 2. **SUPABASE_MIGRATION.md** (✨ новый)
Полная инструкция по миграции на VPS:
- Создание таблиц в Supabase
- Обновление .env на VPS
- Обновление кода
- Тестирование
- Решение проблем

---

### 3. **QUICK_START_SUPABASE.md** (✨ новый)
Быстрый старт в 5 шагов:
1. Создаём таблицы в Supabase
2. Подготавливаешь DATABASE_URL
3. Обновляешь .env на VPS
4. Обновляешь проект (git pull)
5. Перезагружаешь бота

Каждый шаг с точными командами!

---

### 4. **UPDATE_SERVER.md** (✨ новый)
Быстрое обновление проекта на VPS:
- Копируй-вставляй команды
- Проверка логов
- Решение типичных проблем

---

### 5. **MIGRATION_SUMMARY.md** (✨ новый)
Подробное резюме всех изменений:
- Что было сделано
- Архитектурные улучшения
- Сравнение производительности
- Процесс миграции
- Финальный чек-лист

---

### 6. **.env.example** (✏️ обновлен)
Пример .env файла с Supabase переменными:
```env
SUPABASE_URL=https://your_project_id.supabase.co
SUPABASE_KEY=your_supabase_anon_key
DATABASE_URL=postgresql://postgres:password@db.host:5432/postgres
```

---

### 7. **CHANGES.md** (✨ новый - этот файл!)
Описание всех изменений

---

## 📊 СТАТИСТИКА ИЗМЕНЕНИЙ

```
Измененные файлы: 12
├── Python файлы: 9
│   ├── config.py (1 блок добавлен)
│   ├── main.py (2 строки обновлены)
│   ├── database.py (полностью переписан)
│   └── 6 файлов в handlers/ (все обновлены)
│   └── cryptobot.py (обновлен)
│
├── Конфиги: 2
│   ├── requirements.txt (1 строка добавлена)
│   └── .env.example (обновлен)
│
└── Документация: 6

Новые файлы: 8
├── schema.sql
├── SUPABASE_MIGRATION.md
├── QUICK_START_SUPABASE.md
├── UPDATE_SERVER.md
├── MIGRATION_SUMMARY.md
├── CHANGES.md
└── Это может быть вашим README
```

---

## 🔐 БЕЗОПАСНОСТЬ

### Защита данных

**Было:**
- SQLite: локальный файл на VPS
- Блокировки: семафоры ОС
- Бэкапы: ручные

**Стало:**
- PostgreSQL: Supabase с SSL/TLS шифрованием
- Блокировки: встроены в БД (advisory locks)
- Бэкапы: автоматические каждый час
- Firewall: можно ограничить доступ по IP

### Переменные окружения

Все секретные значения хранятся в `.env` и **НЕ** коммитятся в git:
```gitignore
.env  # Не публикуется
.env.example  # Пример - публикуется
```

---

## 🚀 ПРОИЗВОДИТЕЛЬНОСТЬ

### Был SQLite
- ~20 одновременных запросов max
- Синхронные операции (блокируют бота)
- File I/O на диск

### Стал PostgreSQL + asyncpg
- 20 одновременных подключений в пуле
- Асинхронные операции (не блокируют)
- Встроенный кэш Supabase
- Индексы для быстрого поиска

**Результат:** Бот отвечает быстрее, может обрабатывать больше пользователей одновременно.

---

## 🔄 СОВМЕСТИМОСТЬ

### API database.py остался тем же

Все функции имеют те же имена:
```python
create_user
get_user
has_subscription
update_subscription
create_payment
get_promo_code
# и т.д.
```

**Только изменилось:**
1. Добавлен `await` перед каждым вызовом
2. Результаты - словари вместо кортежей
3. Обработка null вместо None

**Значит:** Если захотим поменять БД в будущем, можно будет переписать только `database.py`!

---

## 📦 ЗАВИСИМОСТИ

### Было
```
aiogram>=3.0.0
aiohttp>=3.8.5
python-dotenv>=1.0.0
```

### Стало
```
aiogram>=3.0.0
aiohttp>=3.8.5
python-dotenv>=1.0.0
asyncpg>=0.28.0  ← Новое!
```

**Что это делает:**
- `asyncpg` - быстрый асинхронный драйвер для PostgreSQL
- Поддерживает все современные версии PostgreSQL
- Оптимизирован для asyncio

---

## ✅ ОКОНЧАТЕЛЬНЫЙ ЧЕК-ЛИСТ

### На локальной машине (уже сделано)
- ✅ Обновлены все Python файлы
- ✅ Обновлены requirements.txt
- ✅ Создан schema.sql
- ✅ Созданы инструкции миграции
- ✅ Обновлена документация

### На VPS (нужно сделать)
- ⏳ Создать таблицы в Supabase (schema.sql)
- ⏳ Обновить .env с Supabase переменными
- ⏳ Обновить код (git pull)
- ⏳ Обновить зависимости (pip install --upgrade)
- ⏳ Перезагрузить бота
- ⏳ Проверить логи

---

## 🎉 РЕЗУЛЬТАТ

После этой миграции:
- ✅ БД в облаке Supabase (безопаснее)
- ✅ Автоматические бэкапы (спокойнее)
- ✅ Асинхронная архитектура (быстрее)
- ✅ Может масштабироваться (готово к росту)
- ✅ Веб-интерфейс для управления (удобнее)

**Всё готово к production! 🚀**
